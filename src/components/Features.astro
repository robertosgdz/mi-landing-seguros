---
import type { lazy } from 'astro:schema';
import FaqComponent from './FaqComponent.jsx';
---

<section id="faq" class="py-20 md:py-2 bg-white">
  
  <!-- 
    CONTROL DE MÁRGENES:
    - w-full: Ocupa todo el ancho.
    - px-4: Margen en móvil.
    - lg:pl-0: AQUÍ ESTÁ EL CAMBIO. 
      Al poner 'pl-0', pegamos el contenido a la izquierda del contenedor principal.
      Si lo quieres aún más pegado al borde de la pantalla, tendríamos que quitar el max-w-[1400px].
  -->
  <div class="w-full max-w-350 px-4 lg:pl-0 lg:pr-8 mx-auto md:mx-0 lg:ml-16.5">
    <!-- Nota: He añadido lg:ml-20 arriba para que tengas un control manual global de cuánto moverlo -->

    <!-- TÍTULO -->
    <div class="mb-12 md:mb-20">
      <h2 class="text-5xl md:text-7xl font-bold text-gray-900 tracking-tight">
        PREGUNTAS FRECUENTES
      </h2>
    </div>

    <!-- COMPONENTE DE PREGUNTAS -->
    <div class="max-w-6xl">
      <FaqComponent client:only="react" />
    </div>

  </div>
</section>