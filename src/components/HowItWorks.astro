---
// IMPORTACIONES DE COMPONENTES REACT
import SecureFeatures from '../components/SecureFeatures.jsx';
---
<!-- ============================================
     SECCIÓN: CÓMO FUNCIONA ASEOFI
     ============================================ -->
<section id="how-it-works" class="py-20 md:py-32 bg-white overflow-hidden -mt-32 md:-mt-40 lg:-mt-40 relative z-20">
  
  <!-- CONTENEDOR PRINCIPAL (Ancho máximo 1280px con padding lateral) -->
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    
    <!-- ============================================
         GRID PRINCIPAL (Video + Contenido de texto)
         Responsivo: 1 columna en móvil, 2 columnas en desktop
         ============================================ -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 md:gap-10 lg:gap-12 items-stretch min-h-150">
      
      
      <!-- ============================================
           COLUMNA IZQUIERDA: VIDEO DEMOSTRATIVO
           ============================================ -->
      <!-- aspect-3/4 (móvil/tablet) = Relación 3:4 VERTICAL (más largo) | lg:aspect-auto (desktop) = Altura completa -->
      <!-- mb-15 md:mb-18 = Margen inferior en móvil/tablet (separación) -->
      <!-- lg:-ml-20 lg:w-11/12 = Desplazamiento negativo en desktop para efecto visual -->
      <!-- md:w-1/2 = Tablet: 50% ancho (más estrecho que antes) -->
      <!-- md:max-h-[700px] = Tablet: altura máxima 700px (más alto) -->
      <!-- md:mx-auto = Tablet: centrado -->
      <div class="aspect-3/4 lg:aspect-auto lg:h-full w-full md:w-1/2 md:max-h-175 md:mx-auto lg:mx-0 lg:-ml-20 lg:w-11/12 max-h-150 lg:max-h-none mb-15 md:mb-18 lg:mb-0">
        
        <!-- Contenedor de video con bordes, sombra y esquinas redondeadas -->
        <div class="relative rounded-2xl overflow-hidden border border-gray-100 group h-full shadow-sm">
          
          <!-- Overlay oscuro sutil (z-10 = encima del video) para mejorar legibilidad -->
          <div class="absolute inset-0 bg-black/5 z-10 pointer-events-none"></div>
          <!-- Nota:
            - absolute inset-0 = Cubre todo el contenedor
            - bg-black/5 = Fondo negro con 5% opacidad
            - z-10 = Capa intermedia (encima del video)
            - pointer-events-none = No interfiere con interacciones
          -->
          
          <!-- VIDEO DE DEMOSTRACIÓN -->
          <video 
            class="absolute inset-0 w-full h-full object-cover object-center"
            autoplay 
            loop 
            muted 
            playsinline
            preload="metadata"
          >
            <source src="/secure01.mp4" type="video/mp4" />
          </video>
          <!-- Nota:
            - absolute inset-0 = Posiciona el video dentro del contenedor
            - w-full h-full = Rellena todo el contenedor
            - object-cover = Rellena sin distorsionar (recorta si es necesario)
            - object-center = Centra el video dentro del contenedor
            - autoplay loop muted = Reproducción automática infinita sin sonido
            - playsinline = Reproduce en línea en móvil (sin pantalla completa)
            - preload="metadata" = Carga solo metadatos para optimizar
            - SIN lg:h-[115%] = Desktop mantiene proporción natural sin zoom
          -->
        </div>
      </div>


      <!-- ============================================
           COLUMNA DERECHA: CONTENIDO EXPLICATIVO
           ============================================ -->
      <!-- flex flex-col = Flexbox vertical -->
      <!-- justify-start = Contenido alineado arriba -->
      <!-- pt-0 pb-10 = Sin padding superior, padding inferior 40px -->
      <div class="flex flex-col justify-start pt-0 pb-10">
        
        <!-- CONTENEDOR DEL TÍTULO -->
        <!-- mb-10 = Margen inferior 40px (separación del contenido) -->
        <!-- -mt-5 = Margen superior negativo -20px (ajuste visual) -->
        <div class="mb-10 -mt-5">
          
          <!-- TÍTULO H2 PRINCIPAL -->
          <h2 class="text-4xl md:text-7xl font-bold text-gray-900 leading-tight mb-0 uppercase tracking-tighter">
            Solicita Y Asegura En Minutos
          </h2>
          <!-- Nota:
            - text-4xl (móvil: 36px) → md:text-7xl (desktop: 48px)
            - font-bold = Peso 700 para máximo impacto visual
            - text-gray-900 = Color negro oscuro (#111827)
            - leading-tight = Altura de línea 1.25 (compacta, visual fuerte)
            - mb-0 = Sin margen inferior (control preciso)
            - uppercase = Convierte a MAYÚSCULAS
            - tracking-tighter = Espaciado entre letras -0.05em (más compacto)
          -->
        </div>


        <!-- COMPONENTE REACT: CARACTERÍSTICAS Y FLUJO -->
        <SecureFeatures client:only="react" />
        <!-- Nota:
          - client:only="react" = Este componente solo se renderiza en el cliente (interactivo)
          - Muestra el proceso paso a paso: Solicitud → Cotización → Aceptar/Rechazar
        -->


      </div>


    </div>


    <!-- ============================================
         SECCIÓN: EDUCATIVA - SINGLE-RISK vs GLOBAL-RISK
         (PEGADO A LA IZQUIERDA MÁXIMO)
         ============================================ -->
    <!-- w-screen = Ocupa todo el ancho de la pantalla -->
    <!-- -ml-4 = Margen izquierdo negativo para compensar el padding del contenedor padre -->
    <!-- lg:-ml-35 = Más negativo en desktop (pega más) -->
    <!-- pl-4 = Padding izquierdo para móvil -->
    <!-- lg:pl-16.5 = Padding izquierdo en desktop (alineado con otras secciones pegadas) -->
    <!-- pr-4 = Padding derecho para móvil -->
    <!-- lg:pr-0 = Sin padding derecho en desktop -->
    <!-- py-16 md:py-20 = Padding vertical reducido -->
    <div class="w-screen -ml-4 lg:-ml-35 pl-4 lg:pl-16.5 pr-4 lg:pr-0 py-16 md:py-20">
      
      <!-- TÍTULO PRINCIPAL -->
      <!-- mb-12 md:mb-16 = Margen inferior responsivo -->
      <div class="mb-12 md:mb-16">
        <h2 class="text-5xl md:text-7xl font-bold text-gray-900 tracking-tight">
          SINGLE-RISK VS GLOBAL-RISK
        </h2>
        <!-- Nota:
          - text-5xl md:text-7xl = Tamaño grande (60px → 48px en desktop)
          - font-bold = Peso 700
          - text-gray-900 = Negro oscuro
          - tracking-tight = Espaciado compacto entre letras
          - MAYÚSCULAS para impacto visual
        -->
      </div>


      <!-- CONTENEDOR DE CONTENIDO (Ancho máximo 6xl) -->
      <!-- max-w-6xl = Ancho máximo 1152px -->
      <div class="max-w-6xl">
        
        <!-- CONTENEDOR DE PÁRRAFOS -->
        <!-- space-y-6 = Espaciado vertical 24px entre párrafos -->
        <div class="space-y-6 mb-12">
          
          <!-- PÁRRAFO INTRODUCTORIO -->
          <!-- text-xl = 20px (coherente con features) -->
          <!-- text-gray-700 = Gris oscuro -->
          <!-- leading-relaxed = Altura generosa (1.625) -->
          <p class="text-xl text-gray-700 leading-relaxed">
            En el mercado de seguros de crédito existen dos enfoques completamente diferentes. 
            ASEOFI se basa en seguros <span class="font-semibold">single-risk</span>, 
            una alternativa mucho más flexible que el modelo tradicional <span class="font-semibold">global-risk</span>.
          </p>
          <!-- Nota:
            - text-xl = 20px (mismo tamaño que features data)
            - Introducción al tema
            - Destaca los dos conceptos clave con semibold
          -->


        </div>


        <!-- CONTENEDOR DE COMPARACIÓN (2 columnas en desktop) -->
        <!-- grid grid-cols-1 md:grid-cols-2 = 1 columna móvil, 2 columnas desktop -->
        <!-- gap-8 = Espaciado 32px entre columnas -->
        <!-- mb-8 = Margen inferior reducido -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-8">
          
          <!-- COLUMNA 1: GLOBAL-RISK (Lo tradicional, lo que no queremos) -->
          <!-- border border-red-200 = Borde rojo claro -->
          <!-- bg-red-50 = Fondo rojo muy claro -->
          <!-- p-6 = Padding 24px -->
          <!-- rounded-lg = Esquinas redondeadas -->
          <div class="border border-red-200 bg-red-50 p-6 rounded-lg">
            
            <!-- TÍTULO DEL BLOQUE -->
            <!-- text-xl font-bold = Tamaño 20px, peso 700 -->
            <!-- text-red-800 = Rojo oscuro -->
            <!-- mb-4 = Margen inferior 16px -->
            <h4 class="text-xl font-bold text-red-800 mb-4">
              GLOBAL-RISK
            </h4>
            <!-- Nota:
              - Nombre del modelo tradicional
              - Color rojo para representar restricciones
              - MAYÚSCULAS
            -->


            <!-- LISTA DE CARACTERÍSTICAS (Lo negativo) -->
            <!-- space-y-2 = Espaciado vertical 8px entre items -->
            <!-- text-xl = 20px (coherente con párrafos) -->
            <ul class="space-y-2 text-xl text-gray-700">
              
              <!-- Item 1 -->
              <li class="flex items-start gap-2">
                <span class="text-red-600 font-bold mt-1">✗</span>
                <span>Obligatorio asegurar <span class="font-semibold">toda tu cartera</span> de clientes</span>
              </li>
              <!-- Nota:
                - Símbolo ✗ en rojo (negativo)
                - Destaca la principal limitación
              -->


              <!-- Item 2 -->
              <li class="flex items-start gap-2">
                <span class="text-red-600 font-bold mt-1">✗</span>
                <span>Requiere <span class="font-semibold">facturación mínima</span> muy alta</span>
              </li>
              <!-- Nota:
                - Barrera de entrada para pymes/autónomos
              -->


              <!-- Item 3 -->
              <li class="flex items-start gap-2">
                <span class="text-red-600 font-bold mt-1">✗</span>
                <span>Solo accesible para <span class="font-semibold">grandes corporaciones</span></span>
              </li>
              <!-- Nota:
                - Exclusión de pequeños negocios
              -->


              <!-- Item 4 -->
              <li class="flex items-start gap-2">
                <span class="text-red-600 font-bold mt-1">✗</span>
                <span>Compromiso a <span class="font-semibold">largo plazo</span></span>
              </li>
              <!-- Nota:
                - Falta de flexibilidad
              -->
            </ul>
            <!-- Fin de lista -->
          </div>
          <!-- Fin de columna Global-Risk -->


          <!-- COLUMNA 2: SINGLE-RISK (Lo nuevo, lo que ASEOFI ofrece) -->
          <!-- border border-green-200 = Borde verde claro -->
          <!-- bg-green-50 = Fondo verde muy claro -->
          <!-- p-6 = Padding 24px -->
          <!-- rounded-lg = Esquinas redondeadas -->
          <div class="border border-green-200 bg-green-50 p-6 rounded-lg">
            
            <!-- TÍTULO DEL BLOQUE -->
            <!-- text-xl font-bold = Tamaño 20px, peso 700 -->
            <!-- text-green-800 = Verde oscuro -->
            <!-- mb-4 = Margen inferior 16px -->
            <h4 class="text-xl font-bold text-green-800 mb-4">
              SINGLE-RISK (ASEOFI)
            </h4>
            <!-- Nota:
              - Nombre del modelo de ASEOFI
              - Color verde para representar beneficios/solución
              - MAYÚSCULAS
            -->


            <!-- LISTA DE CARACTERÍSTICAS (Lo positivo) -->
            <!-- space-y-2 = Espaciado vertical 8px entre items -->
            <!-- text-xl = 20px (coherente con párrafos) -->
            <ul class="space-y-2 text-xl text-gray-700">
              
              <!-- Item 1 -->
              <li class="flex items-start gap-2">
                <span class="text-green-600 font-bold mt-1">✓</span>
                <span>Asegura <span class="font-semibold">solo las facturas que necesites</span></span>
              </li>
              <!-- Nota:
                - Símbolo ✓ en verde (positivo)
                - Principal ventaja: libertad de elección
              -->


              <!-- Item 2 -->
              <li class="flex items-start gap-2">
                <span class="text-green-600 font-bold mt-1">✓</span>
                <span><span class="font-semibold">Sin requisitos mínimos</span> de facturación</span>
              </li>
              <!-- Nota:
                - Accesible para todos los tamaños
              -->


              <!-- Item 3 -->
              <li class="flex items-start gap-2">
                <span class="text-green-600 font-bold mt-1">✓</span>
                <span>Para <span class="font-semibold">pymes, autónomos y grandes empresas</span></span>
              </li>
              <!-- Nota:
                - Inclusividad del modelo
              -->


              <!-- Item 4 -->
              <li class="flex items-start gap-2">
                <span class="text-green-600 font-bold mt-1">✓</span>
                <span><span class="font-semibold">Total flexibilidad</span> sin compromisos</span>
              </li>
              <!-- Nota:
                - Libertad y adaptabilidad
              -->
            </ul>
            <!-- Fin de lista -->
          </div>
          <!-- Fin de columna Single-Risk -->


        </div>
        <!-- Fin del grid de comparación -->


        <!-- PÁRRAFO CONCLUSIVO -->
        <!-- text-xl = 20px (coherente con párrafo introductorio) -->
        <p class="text-xl text-gray-700 leading-relaxed">
          ASEOFI te permite <span class="font-semibold">proteger tus facturas a crédito de forma individual</span>, 
          sin obligaciones de cartera completa ni mínimos. Solo asegura lo que realmente necesites, 
          cuando lo necesites, con total transparencia y flexibilidad.
        </p>
        <!-- Nota:
          - text-xl = 20px (mismo tamaño que otros párrafos)
          - Resumen del diferenciador ASEOFI
          - Mensaje persuasivo sobre beneficios
          - Basado 100% en información del PDF
        -->


      </div>
      <!-- Fin del contenedor de contenido -->


    </div>
    <!-- Fin de sección Single-Risk vs Global-Risk -->


    <!-- ============================================
         BLOQUE INDICADOR DE RIESGO
         ============================================ -->
    <!-- w-full = Ancho completo -->
    <!-- max-w-3xl = Ancho máximo 768px (legibilidad) -->
    <!-- mx-auto = Centrado horizontalmente -->
    <!-- mt-16 = Margen superior reducido (menos separación) -->
    <div class="w-full max-w-3xl mx-auto mt-16">
      
      <!-- TÍTULO DEL INDICADOR -->
      <h3 class="font-bold text-black text-sm mb-3">
        Indicador de riesgo del producto ASEOFI
      </h3>
      <!-- Nota:
        - font-bold = Peso 700
        - text-black = Color negro puro (#000000)
        - text-sm = Tamaño 14px (etiqueta/contexto)
        - mb-3 = Margen inferior 12px (separación de la caja)
      -->
      
      <!-- CAJA CONTENEDORA DEL INDICADOR -->
      <!-- border border-black = Borde negro 1px en todos los lados -->
      <!-- p-4 = Padding interior 16px en todos los lados -->
      <!-- flex gap-4 items-start = Flexbox horizontal, separación 16px, alineación arriba -->
      <!-- bg-white = Fondo blanco -->
      <div class="border border-black p-4 flex gap-4 items-start bg-white">
        
        <!-- CONTENEDOR DE LOS NÚMEROS (1/6) -->
        <!-- flex items-baseline = Flexbox horizontal, alineación por línea de base -->
        <!-- gap-1 = Espacio mínimo entre números (4px) -->
        <!-- shrink-0 = No se comprime en pantallas pequeñas (mantiene tamaño) -->
        <div class="flex items-baseline gap-1 shrink-0">
          
          <!-- NÚMERO 1 (más claro) -->
          <span class="text-4xl font-bold text-gray-300">1</span>
          <!-- Nota:
            - text-4xl = 36px (tamaño grande para impacto)
            - font-bold = Peso 700
            - text-gray-300 = Color gris claro (#d1d5db) - visual de jerarquía
          -->
          
          <!-- SEPARADOR / -->
          <span class="text-4xl font-bold text-black">/</span>
          <!-- Nota:
            - text-black = Color negro (#000000)
            - Mismo tamaño que los números (36px)
          -->
          
          <!-- NÚMERO 6 (oscuro) -->
          <span class="text-4xl font-bold text-black">6</span>
          <!-- Nota:
            - text-black = Color negro (#000000)
            - items-baseline garantiza alineación consistente con otros elementos
          -->
        </div>


        <!-- CONTENEDOR DE TEXTO EXPLICATIVO -->
        <!-- text-xl = 20px (coherente con párrafos) -->
        <!-- text-black = Color negro -->
        <!-- leading-relaxed = Altura de línea 1.625 (legible, espaciada) -->
        <!-- space-y-2 = Espaciado vertical 8px entre párrafos -->
        <!-- pt-1 = Padding superior 4px (alineación visual con números) -->
        <div class="text-xl text-black leading-relaxed space-y-2 pt-1">
          
          <!-- PÁRRAFO 1: EXPLICACIÓN DEL INDICADOR (Del PDF) -->
          <p>
            <span class="font-bold">Este número es indicativo del riesgo del producto,</span> 
            siendo 1/6 indicativo del menor riesgo y 6/6 del mayor riesgo.
          </p>
          <!-- Nota:
            - <span class="font-bold"> = Destaca la frase principal (peso 700)
            - Texto 100% exacto del PDF
            - text-xl = 20px (coherente con el resto)
          -->
          
          <!-- PÁRRAFO 2: INFORMACIÓN SOBRE ASEOFI (Adaptado del PDF) -->
          <p>
            ASEOFI ofrece seguros de crédito individual single-risk con total transparencia. 
            Nuestro objetivo es permitir que pymes y autónomos aseguren solo las facturas que necesiten, 
            sin obligación de contratar seguros globales ni cumplir requisitos de facturación mínima.
          </p>
          <!-- Nota:
            - Este párrafo reemplaza el contenido sobre garantías de depósitos
            - Contenido 100% basado en información del PDF
            - Comunica el valor principal de ASEOFI (sin inventar datos de regulación)
            - text-xl = 20px (coherente con el resto)
          -->
          
        </div>
        <!-- Nota:
          - Combinación de números grande + texto pequeño = jerarquía visual clara
          - Fácil de escanear rápidamente
        -->


      </div>


    </div>


  </div>


</section>
